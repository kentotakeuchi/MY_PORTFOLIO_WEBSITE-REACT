import React from 'react';
import classes from './Header.css';

const header = () => (
    <div className={classes.Header}>
        <p>kento takeuchi</p>
        <p>web developer / santa clara</p>
    </div>
);

export default header;